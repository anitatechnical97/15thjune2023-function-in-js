 //   var function;                 // Invalid variable declaration.

        // 1. Function definition

        function getMyAddress(address) {
            console.log(address);
        }
    
          // 2. Calling the function (Function Call).
 
        getMyAddress("Mumbai");      // parameter which we pass at the time of calling of function is called "actual argument".
    